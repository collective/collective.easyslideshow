 * instead of site-wide properties, apply a tab on a 
   slideshow folder.
 * slideshow currently uses registerIndexableAttribute,
   but this is deprecated, and completely removed in Plone 4.
   Needs to be updated.
